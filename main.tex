\documentclass[thesis]{cluu}

\usepackage[style=cluu]{biblatex}
\addbibresource{pal.bib}

\begin{document}
\title{Analysis and Detection of LLM-Generated Text in Swedish}
\author{Gina Welsh}
% \date{...}
\supervisor{Birger Moëll, Uppsala University}
% Use \supervisor (without s at end) if you have only one

\maketitle

\begin{abstract}

This thesis investigates investigate systematic differences
between text generated
by large language models (LLMs) and human-written text in the
Swedish language. In addition, the
thesis analyses significant differences in the ability to detect
LLM-generated
Swedish text between humans and AI-detection tools. The study
will include both a formal and informal text
component, that is, linguistic analyses and AI-detection
experiments will be performed on two sources
of data from formal and informal domains.
\end{abstract}

\tableofcontents

\addchap{Preface}
I would like to thank the following people...
% with \addchap instead of \chapter it isn't numbered.

\chapter{Introduction}
The invention of transfomer-based Large Language Models (LLMs)
has created a new wave of textual data that is entirely
generated by machines and very closely emulates, to the naked
eye, text written by humans. As these tools continue to generate
unbelievable amounts of textual data at a break-neck speed (<<
insert some figure taken recently on number of TB per year >>),
changing the landscape of data proliferated through the internet
and in other published textual sources, such studies could also
shed light on what kind of text is being read by the literate
population, which styles are dominating, and what kind of
language structures and content is being prioritised through the
usage of these generative AI. In addition, studies such as these
can inform readers of potential heuristics to use when reading a
text, influencing their intuition to scrutinise a text and
potentially infer their origin from a machine. Analysing the
characteristics of LLM-generated data enhances the ability to
shed light on the mechanisms of LLMs, potentially contributing
to transparency and LLM-probing research within the NLP space.

In addition, there has been emerging research into detection
methods of LLM-generated text, both by AI detection tools and by
humans. These studies help with development of AI detection
tools and to further educate the general public in building
their intuition of LLM-generated text. There is recent evidence
that LLM-generated text has changed the make-up of text on the
internet (INSERT SOURCES), with an estimated (SOURCE) \% of the
internet estimated to be made up of LLM-generated text (although
this figure is only estimated as it is difficult to gauge).
AI-detection tools are useful in several contexts, including
plagiarism detection and the propagation of LLM-generated text
containing political propaganda and hate speech to influence
users on the internet. These topics are pressing matters due to
the number of users on the internet and the risk of political
order being compromised and bias against groups being reinforced
with the help of LLM tools, which can create massive amounts of
data very quickly.

The study of the linguistic properties of LLM-generated text has
emerged from this recent phenomenon as a growing topic of
natural language processing (NLP). Recent research has focused
primarily on English-language textual analysis as the dominant
type of data analysed. However, there has been thus far limited
but emerging research into the characteristics of LLM-generated
text in languages other than English, such as Swedish. Hence,
there is a lot of opportunity for exploration of this topic in
many other languages.



\section{Research questions}

The intended purpose of this study is to investigate systematic
differences (if any) between text generated
by large language models (LLMs) and text written by humans in
the Swedish language. In addition, the
study will investigate whether there are any significant
differences in the ability to detect LLM-generated
Swedish text between humans and LLM tools. The study will
include both a formal and informal text
component, that is, linguistic analyses and AI-detection
experiments will be performed on two sources
of data from formal and informal domains.

\begin{enumerate}
\item Are there systematic differences between LLM-generated and
human-written formal text in
Swedish?
\item Are there systematic differences between LLM-generated and
human-written informal text in
Swedish?
\item Are there differences between humans and AI-based
detection tools in their ability to detect LLM-
generated Swedish text?
\end{enumerate}

\section{Experimental-set up of the thesis}

The thesis is comprised of two experiments: 1) textual analysis,
and 2) LLM detection. The first experiment focuses on how
human-written and LLM-generated text differ in surface-level and
content-level characteristics. There are two domains of text
used: 1) a formal domain comprised of Bachelor's level thesis
abstracts, and 2) an informal domain of internet thread
comments. The LLM used to generate the texts is OpenAI's
GPT-5.2.
The second experiment focuses on the differences in ability and
thinking process between humans and LLMs in detecting
LLM-generated text. The LLM of focus is DeepSeek (? plus model
version), and the accuracy and reasoning process will be
evaluated for both Swedish-native speakers and this LLM in
detecting both the formal and informal data.



\chapter{Background}
\paragraph{}
This chapter provides an overview of previous studies
related to the topics of LLM development, textual analysis, and detection. First, it will outline the development of
large language models and their text generation abilities for
English and Swedish. Second, it will describe previous research
comparing AI-generated text with human-written text. The chapter
will then outline tools and human participant studies related to
AI-generated text detection.
\section{Transformer-based Large Language Models}
[Purpose: introducing the type of language model referred to in thesis, some architectural characteristics, and cross-task abilities, NEED ADD CITES]
\paragraph{}
In this thesis, the term "Large Language Models" refers to those that are transformer-based, that is, based on a neural network architecture with multi-head attention mechanisms. This type of
architecture was first proposed in \textcite{vaswani2017attention}, where the authors developed an architecture that did not rely on earlier recurrent neural architectures and demonstrated superior
performance and lower training time for a variety of tasks, including text generation, including optical recognition, speech processing, and text generation (CITE Islam 2023, Radford). The strength of
transformers in their adaptation to a wide variety of tasks lies in their ability to handle long dependencies between different elements of input sequences, meaning that it can calculate how relevant
different parts of input are to one
another, no matter how far apart one element is from another
within a sequence. This ability has shown to improve the fluency and coherence of text, as well as emergent abilities that are not found in earlier language models, such as instruction-following
(CITE) and reasoning (CITE). Examples of transformer-based large language models include GPT-5 (GET PAPER 5; https://arxiv.org/pdf/2303.08774v3), Claude Sonnet (GET PAPER), and LLaMa (GET PAPER).

\section{LLM development methodology}
[Purpose: Detailing the parameters, data sources of LLMs; training process]
\paragraph{}
Compared to earlier statistical and neural network models, LLMs that have been released after GPT-3 are trained on very large amounts of data. Transformer-based LLMs for text generation normally
contain tens to thousands of billions of parameters, pre-trained on diverse sources of textual data such as Web crawl data (CITE), online encyclopedias, news outlets, academic articles, books, and
code repositories. The scale of data and parameters varies by each LLM and release version. During model development, the data collected is then often filtered to enhance data quality, relevance and
generalisation of the data to different user inputs (CITE1, CITE2, \parencite{Brown2020LanguageMA}).

\subsubsection{LLMs and the Swedish language}
Several studies have developed datasets, models and evaluation methods specifically for Scandinavian languages. The most notable dataset is the Nordic Pile (Öhman et al., 2023), a 1.2 terabyte dataset trained primarily on Swedish, Norwegian, Danish, Icelandic, Faroese, and smaller subset of high-quality English data. The Nordic Pile was used to train GPT-SW3 (Ekgren et al., 2024), the first Scandinavian GPT model, the largest largest release model of which has 40 billion parameters at the time of writing. Further studies that incorporate GPT-SW3 in their pipelines include Olsén (2024), which focuses on instruction-tuning for Swedish language tasks, and (INSERT ANOTHER GPT-SW3 STUDY). AI Sweden has also released a model that further pretrains Llama (cite) with a subset of the Nordic Pile (footnote https://huggingface.co/AI-Sweden-Models/Llama-3-8B).

Existing evaluation benchmarks that are specifically tailored for LLM-generated Swedish include ScandEval (Nielsen, 2023), which offers several metrics including Swedish named entity recognition (NER), sentiment classification, and linguistic acceptability; a Sweden-related factual knowledge benchmark dataset by Kunz (2025);  SweSat (Kurfali et al. 2025), a benchmark from the Swedish university entrance exam (Högskoleprovet); and the Swedish Medical LLM Benchmark (Moëll et al. 2025), a framework to evaluate LLM capabilities for Swedish medical texts. These benchmarks can be evaluated on large-scale multilingual LLMs to test their performance specifically for the Swedish language and cultural context.

\subsubsection{Multilingual LLMs}
[Purpose: this thesis uses a multilingual LLM, need to know what cross-lingual capabilities transfer to Swedish, and what limitations it has. Cultural limitations? To guide the discussion of how the
English-centric LLM could have limitations in the syntax and cultural information of the Swedish performance and AI detection reasoning.] Latest-release, large-scale LLMs such as OpenAI's GPT, Anthropic's Claude, and Google's Gemini are trained on data from multiple languages and therefore have multilingual capabilities (CITE OA, CITE AN, CITE GE). It is difficult to ascertain the exact amount of linguistic diversity within the training data of large-scale multilingual LLMs, as there is no publicly-accessible, detailed breakdown of the languages
represented in the training data of the latest versions of large-scale multilingual LLMs, such as OpenAI's GPT, Anthropic's Claude, and Google's Gemini (check more open source ones?). 

However, an approximation of large-scale multilingual LLM's linguistic diversity can be obtained through other openly-accessible sources. The original paper that proposed the GPT-3 autoregressive language model (CITE; Brown et al. 2020) released a dataset detailing its multilingual distribution; 92.6\% of the total training data's word count was in English; the next languages were  The Swedish word count proportion was 0.11\%. In addition, the Common Crawl monthly archives, an
open repository of web data cited as a major source in several large scale LLMs (CITE1, CITE2, CITE3), calculates the percentage of Swedish data within its corpus as between 0.68 - 0.72 \% across
three versions released in 2026 (https://commoncrawl.github.io/cc-crawl-statistics/plots/languages). Overall, these sources indicate a proportion of Swedish training data within the large-scale,
English-centric LLMs to be well below 1\%.

Despite the asymmetrical language proportions in their data, LLMs display emergent multilingual properties in pattern recognition of the linguistic structures of supported languages, displaying a high
level of fluency across a range of natural language understanding and generation benchmarks (cite: Kew et al., 2024; Lai et al, 2023; Holmstrom et al., 2023; need more recent cites). On the other
hand, since LLMs embed cultural norms and biases inherent in their training, they require benchmarking of not only linguistic fluency, but also cultural alignment - that is, the cultural references,
expectations and biases of non-Anglocentric cultures. While there is also considerable evidence that LLMs can in fact successfully reflect the cultural contexts of non-Anglocentric cultures, some show
that LLMs' performance are still stronger in English than other languages for reasoning tasks (Liu et al, 2024), figurative proverbs (Liu et al., 2024), and (insert other cites with tasks).
\paragraph{}
"Multilingual considerations demand increased attention. Specifically, existing
benchmarks are mainly developed for English datasets, with only a few like MULTI-
TuDE (Macko et al. 2023) and M4 (Wang et al. 2023b) covering multiple languages, in-
cluding Arabic, Catalan, Chinese, Czech, Dutch, English, German, Portuguese, Russian,
Spanish, Indonesian, Bulgarian, and Ukrainian. However, there is still a significant lack
of datasets for other language resources, especially low-resource languages." - Wu et al, 2024

- need to add all studies that include the Nordic pile as a source.

\subsubsection{LLMs and cultural transfer}
Given the dominance of English-centric multilingual LLMs, emerging result into the cultural transfer  cultural transfer of English-centric multilingual LLMs to other languages smaller languages like Swedish, spoken primarily in a geographically smaller area (in the case of Swedish: Sweden, Finland). Holmström et al. (2023) examined the cultural adaptation of English-centric pre-trained GPT-3 LLMs to the Swedish context. Their findings indicated that despite LLMs' exposure to limited Swedish during training, they demonstrate a high capability to culturally adapt to the Swedish context "off-the-shelf" without unwanted over-generalisation of Swedish cultural aspects.


\section{LLM textual analysis studies}
[Purpose: give a breakdown of previous studies that have textually analysed the output of LLMs, or put together signs of LLM-generated writing. First start with English (and other languages), then
focus on Swedish studies]
\paragraph{}
These studies are focused on increasing transparency of signs of AI writing for the general public. (need more introduction). 
\paragraph{}
- "Additionally, LLM-generated text often conveys less emotional intensity and exhibits clearer presentation than human writing.  This phenomenon is possibly influenced by inherent positive bias in LLMs (Giorgi, Ungar, and Schwartz 2021; Markowitz, Hancock, and Bailenson 2023; Mitrovi´c, Andreoletti, and Ayoub 2023)"
\paragraph{}
Munoz-Ortiz et al. (2024) compared the linguistic components of
human and LLM-generated news articles and compared their
patterns among six generative LLMs. For their prompting
strategy, they asked the models to generate a news paragraph
based on the news article headline its first words. For their
linguistic analysis, they compared the human-written and
LLM-generated text groups through differences in their
linguistic structures, including vocabulary distribution,
part-of-speech tag distributions, dependency arc lengths,
constituent lengths, cosine similarity, and emotional category
tagging. The results of this analysis showed significant
differences between the human-written and LLM-generated text on
all textual analysis metrics. A sentiment analysis of the models found less of an inclination to display aggressive negative emotions (such as fear or anger) than humans. This finding could be influenced by inherent positive bias in LLMs (Wu et al., 2024; Giorgi et al., 2021; Markowitz et al, 2023), which be a result of safety-policy filtration of negatively-biased data within LLM training (CHECK - Liao et al., 2023?). 

Liao et al. (2023) compared
human-written and ChatGPT-generated medical texts by their vocabulary
distribution, part-of-speech tag distribution, dependency arcs,
sentiment analysis, and perplexity. Their results found that human-written
medical texts contained more concrete information, had higher
vocabulary diversity, and contained information that human
experts would consider more useful. The ChatGPT-generated
medical texts appeared more fluent and logical, but expressed
more generalised terminology rather than specific, effective
information for the problem's context, which could be attributed to  human-written medical text being more tailored to situational modelling of the particular medical case (Mahowald et al., 2024).
\paragraph{}
- need add more synthesis

\section{LLM-detection studies}

\subsection{LLM-detection by humans}
Landberg (2024) investigated the effectiveness of AI chatbots to
detect texts created by AI in Swedish and English. The main
component of the study A recent study investigated LLM reasoning
in Swedish. Möell and Boye (2025) studied the performance of
LLMs on language complexity measurement tasks (LIX readability
metric and Average Dependency Distance). The results of the
study found that all models demonstrated at least come capacity
for each of these tasks, but that LLMs remain prone to errors in
explicit calculations and syntactic analysis. [need add more info; Wikipedia signs of AI writing referenced studies]
\subsection{LLM-detection by LLMs}

\subsubsection{LLM reasoning}
Previous work has also shown evidence of a distinction between
the reasoning patterns of LLM tools when compared to those of
humans. (CITE) evaluated the medical reasoning capabilities of
DeepSeek R1, computing its outputs to the reasoning patterns of
medical domain human experts. The study performed qualitative
and quantitative analyses of AI- and human-medical reasoning for
100 clinical cases from the MedQA dataset. The study found a
93\% diagnostic accuracy for DeepSeek R1's performance. Many of
the LLM's errors were attributed to over-focus on the details of
a medical problem, rather than a more holistic and intuitive (?)
understanding by humans, as well as an insufficient
understanding of medical protocols when compared to human
experts. The study also found a strong correlation between the
length of output and the error rate of LLMs; t hat is, results
from LLMs that were longer were more likely to contain incorrect
diagnoses or medical details.

- need more studies here!!

\section{Background summary}

[insert summary]

\chapter{Methodology}

The methodology is comprised of two main parts: 1) a comparison
of textual elements between human-written Swedish and
LLM-generated Swedish, and 2) a comparison between the detection
rates of humans and LLM-detection of LLM-generated Swedish. Both
parts of the methodology comprise of a human component and an
LLM-generated component.

- why choose GPT-5.2 multilingual instead of SW3? GPT-5.2 was one of the top k performers for the Swedish language (CITE), with a massive estimated number of parameters (cite number, source). SW3, on
the other hand, only had x number of parameters. The study did not want to have number of parameters as a confounding variable for the results of the study, and also didn't want to compare between LLM
architectures without having a large-scale amount of Swedish human-written data



\section{Human-written data collection}

The data collection of this thesis formed the data source of
both the textual analysis and LLM detection experiments. The
dataset comprises of 1) a set of texts in a formal register,
collected through abstracts; and 2) a set of texts in an
informal register, collected through forum comments. These two
registers are selected to measure differences in textual
elements and detection rates of LLM-generated text within two
different contexts.

\subsection{Human-written abstracts}

The formal human-written dataset comprises of (n=142(?))
Bachelor's level thesis titles, abstracts and keywords sourced
from the Digitala Vetenskapliga Arkivet (DiVA) portal. All
Swedish abstracts and keywords were written originally in
Swedish. All abstracts were sourced between 2007-2017, to negate
the chance of of any transformer-based generative models
influencing any part of the dataset's titles or abstracts. The
topics of the thesis abstracts were sampled randomly across
(insert number) topics. On average, the thesis abstracts were
(n=words) in length.

\subsection{Human-written internet comments}

-- THE DATA COLLECTION FOR THIS NEEDS TO BE RE-DONE

The informal human-written dataset, to measure the Swedish
language in a more casual register, was comprised of 203 comment
replies to (insert n=questions) Swedish-language Reddit
questions, retrieved across subreddit forums r/Sweden,
r/svenskpolitik, r/Stockholm and r/Goteborg. The average length
of comments was (n=comment length). To mitigate the possibility
of transformer-based generative AI tools being involved in the
creation of the subreddit's questions and replies, only
questions and comments with a timestamp before January 2017
(2017-01-01) were scraped via the PullPush API (cite or
footnote). To ensure that the post's titles were in question
form and in Swedish, the scraper script checked for the presence
of a question mark ("?") and used the <langdetect> Python
library to identify the post as being written in Swedish ("sv").
On average, the Reddit comments

\section{LLM data generation}

All LLM-generated data for this study was created by OpenAI's GPT-5.2 multilingual model (cite). GPT-5.2 was selected for this study, since at the time of writing, this model was one of the top k performers for the Swedish language (CITE), with a estimated number of parameters (n = cite number, source). GPT models are also widely used by the community, with the number of users estimated as (n=n), which would more closely simulate a real-life scenario of using LLMs to generate text.  AI Sweden's SW3 is trained on Scandinavian data; however, the model at the time of writing was trained on x number of parameters, making it incomparable to the widely-used large-scale models (GPT, Claude, Gemini). The study did not want to have the number of parameters as a confounding variable for the results of the study. The study also chose one model as the source of text generation, since a systematic comparison of the results between different LLM architectures without a larger-scale amount of Swedish human-written and generated data would have reduced the statistical power of the results.

\section{Abstract data}

The formal human-written dataset comprises of an LLM-generated
version of each human-written sample of the DiVA
Bachelor's-level thesis abstracts. The model used was OpenAI's
GPT 5.2 model (<gpt-5.2>). The prompt used as input to generate
the comments was:

[INSERT BREAKDOWN OF GPT 5.2 DATA]

https://arxiv.org/pdf/2601.03267
- trained on diverse datasets, 

https://apxml.com/models/gpt-52-high

- 
\paragraph{}
"Titta på titeln och nyckelorden och skapa en sammanfattning i
kandidatuppsatsstil med dina egna ord: <title, keywords>."
\paragraph{}
English translation: Look at the title and keywords and create a
summary of the thesis abstract in your own words: <title,
keywords>
\paragraph{}
-  The LLM generation prompt also did not use additional evasion tactics
such as "write it in the style as close as possible to a native Swedish speaker" or "use a more diverse amount of vocabulary". While these would be very interesting and useful experiments in further
work on this topic, this study focused on a minimal prompt to control for evasion tactics, and to focus on the "bare-bones" / zero-shot generation of the Swedish text.

\subsection{LLM-generated informal data}


\section{Experiment 1: textual analysis}

The aim of the textual analysis experiments is to investigate
meaningful differences between LLM-generated and human-written
Swedish. This method is based on the hypothesis that there are
indeed meaningful differences in the writing style between the
two textual sources in Swedish. The method aims to cover both
surface-level analyses of the text, that is, investigating
structural differences between the word forms and surface-level
composition between the two modes; as well as content-level
analysis, that is, sentiment analysis, entities, and themes of
the texts.
\subsection{Surface-level analysis}
For the surface-level analysis, the spaCy package was used
(\texttt{sv\_core\_news\_sm}) to collect distributions of:
vocabulary distribution, part-of-speech distribution, dependency
analysis (sprakbanken?), dependency arch length, average word
length, average sentence length.

\subsection{Content-level analysis}

For the content analysis, the study used the sentiment analysis,
named entity recognition, and thematic clustering(?) techniques
to uncover recurring ideas and themes within both the abstracts
and comments. Hypothesis: this is going to be more important for
the Reddit comments than the abstracts.


\section{Experiment 2: LLM-generated text detection}


\subsection{LLM-detection experimental set-up}

Another LLM (DeepSeek or whatever) to do AI detection -
quantitative measure (accuracy), qualitative measure (reasoning)

\subsection{Human detection} 
For the human detection experiment, a web game was created by
the author with 15 Reddit-style forum threads with 4 comment
replies per question, and 15 thesis abstract questions. The URL
of this web game was shared in Swedish-speaking forums, with the
only prerequisite screening for users of the game was
native-level knowledge of Swedish.
\paragraph{}
Before the game started, the user was prompted to fill in
information about 1) their age, 2) their occupation, and 3) the
estimated number of hours of their LLM tool usage per week. Each
thread and abstract was shown one at a time, with instructions
given to the user to determine whether each comments or abstract
was LLM-generated or human-written. The proportion of AI
comments or abstracts to human-written was sampled at random.
After each question was completed, feedback was given
immediately to the user as to whether their response was correct
or not before moving on to the next round. This immediate
feedback characteristic was decided in order to increase the
chance of engagement by the user until the very end. Each user
was given the option to exit the game at any time. At the end of
the game session (or upon exit), the user was shown their
accuracy results and prompted to fill in an optional short
reflection to explain their reasoning process for LLM-detection
throughout the game.
\paragraph{}
There were (n=?) game sessions submitted through the web game,
each of which was stored in a Google Sheet form with information
about their usage time-stamp, question identification number,
which questions were determined as human, and accuracy scores.
The results were measured in terms of accuracy (F-score too?),
and qualitative analysis of any reasoning descriptions sent
through the game.

\subsection{AI detection}

- the LLM detection prompt did not mention that the text was generated by GPT, even though this is mentioned as a potentially useful prompt in Wu et al. 2024. This was to closely simulate a real-life
scenario where an LLM check is being used on online data, where the original model used to generate the text would not be known.

\chapter{Results}
text here


\chapter{Discussion}


Textual analysis:

- potential points of discussion:

Useful for Limitations
- Category was completely human written and completely LLM written, without mixed text examples, which is a plausible scenario (Wu et al., 2024, more)
- Human-written data was taken between 2007 - 2017, which did guarantee that no examples had any transformer-based LLM as help; however, human-written text written then could have outdated information or cultural references that mark the text as older and may be not so comparable for studies that use recent human-written data as examples. 
- Another issue that has not been fully discussed is the human-edited text and LLM-assisted text. For example, after applying LLM to generate a text, humans often edit and modify certain words or passages (Wu et al., 2024). Conversely, humans may write an original text themselves before employing LLMs to modify the text's content and structure. Current mainstream LLM detectors show evidence of degrading in performance when categorising paraphrased text (Wolff 2020) This study only used generation techniques that completely generate a passage themselves using the title and key words (not re-writing the human written examples), but this technique would be less comparable for studies that look at partially LLM-assisted text. However, this type of study would be interesting to conduct in future research. 




\chapter{Conclusion}
text here

%\nocite{*}

\printbibliography
% at start yields errors because there are no citations and the
bibliography file is empty

\end{document}
